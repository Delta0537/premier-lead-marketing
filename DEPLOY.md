# Deploy Premier Lead Marketing Website

## 🚀 Quick Deploy Commands

**Open PowerShell** (right-click Start menu → Windows PowerShell or Terminal)

### **Step 1: Navigate to website folder**
```powershell
cd "C:\ideas\Digital Marketing\Premier Lead Marketing\website"
```

### **Step 2: Check status**
```powershell
git status
```

### **Step 3: Add changes**
```powershell
git add index.html
```

### **Step 4: Commit**
```powershell
git commit -m "Add Google Tag Manager tracking"
```

### **Step 5: Push to GitHub**
```powershell
git push origin main
```

**Vercel will auto-deploy in 1-2 minutes!** ✅

---

## 📋 Full Command Sequence (Copy/Paste All At Once)

```powershell
cd "C:\ideas\Digital Marketing\Premier Lead Marketing\website"
git status
git add index.html
git commit -m "Add Google Tag Manager tracking"
git push origin main
```

---

## 🆘 Troubleshooting

### **"Not a git repository"**
You need to initialize git:
```powershell
git init
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
```

### **"Author identity unknown"**
Set your git identity (one-time):
```powershell
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"
```

### **"Repository not found"**
Make sure you have a GitHub repo created first!

---

**Run these in PowerShell NOW!** 🚀
